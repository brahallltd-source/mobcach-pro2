/**
 * Consolidated Arabic labels for the agent navigation rail.
 * The actual sidebar links (including **سجل الطلبات** → `/agent/requests-history`) are built in `SidebarShell` / `components/ui.tsx` (`agent` nav array).
 */
export { SIDEBAR_AGENT_AR } from "@/lib/constants/i18n";
